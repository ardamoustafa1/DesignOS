Start a DesignOS intake for: $ARGUMENTS

You are running the intake stage that every workflow begins with. Do NOT design yet.

1. **Check what's already known:** read project `memory/client.md` and `memory/brand.md`
   if they exist — never re-ask answered questions.

2. **Compile the contract** using `DesignOS/brain/brief-compiler.md` (ask only the
   high-impact missing answers, all at once, briefly):
   1. The product in one sentence?
   2. Who converts — the one-reader sentence? ("This would have saved us during…")
   3. The ONE conversion/primary action?
   4. What real proof exists? (logos, numbers, quotes, screenshots — inventory it;
      missing proof gets flagged, never invented)
   5. Sector? (→ routes the `DesignOS/industries/` file, which sets theme, tone,
      trust requirements, motion license)
   6. What does the product actually process or produce (screens, workflows, data
      shapes, inputs/outputs), and which assets can be shown?
   7. What belief can the brand credibly own, and what must the result never resemble?
   8. Which states, breakpoints, themes, locales, and measured checks define done?

   If the user answers partially, apply the 70% rule
   (`DesignOS/brain/decision-framework.md`): state your assumptions explicitly and
   proceed.

3. **Write memory:** bootstrap `memory/` from `DesignOS/memory/templates/` if absent;
   fill `client.md` (including the one-reader sentence) and whatever of `brand.md` the
   answers gave. Decisions carry reasons and dates.

4. **Derive and route:** for full pages, compare three materially different directions
   (material-led, audience-led, belief-led), select one with reasons, then name the workflow
   (`DesignOS/workflows/landing-page.md`, `new-project.md`, or
   `component-request.md`), list the modules the kernel's table routes for this task,
   and state the plan in 3–5 lines. Then begin the Design Loop.
